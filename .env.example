# local = seed + browser localStorage overrides (default)
# mongodb = API routes use MongoDB/Mongoose
PAINTING_DATA_SOURCE=local
MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster.mongodb.net/zvi-aharon-art
